# AWS-S3-Bucket-Replication
Achieve robust data durability and Disaster Recovery (DR) by setting up S3 Cross-Region Replication (CRR). This guide uses the AWS Management Console to show you how to provision two S3 buckets, enable Versioning, create the essential IAM Role/Policy, and configure the replication rule for automatic, real-time data mirroring across AWS regions.
